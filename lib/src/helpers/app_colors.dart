import 'package:flutter/material.dart';

class AppColors {
  // Primary and Secondary Colors
  static const Color primaryTextColor = Color.fromRGBO(255,101,0,1);
  static const Color blackTextColor = Colors.black;
  static const Color whiteTextColor = Colors.white;
  static const Color greenTextColor = Color.fromRGBO(0, 255, 85, 1);

  static const Color secondary = Color(0xFFFFC107); // Amber
  static const Color accent = Color(0xFF4CAF50); // Green
  // Background Colors
  static const Color backgroundLight = Color(0xFFF5F5F5);
  static const Color backgroundDark = Color(0xFF121212);

  // Surface Colors
  static const Color surfaceLight = Colors.white;
  static const Color surfaceDark = Color(0xFF1E1E1E);

  // Text Colors
  static const Color textPrimaryLight = Color(0xFF212121);
  static const Color textSecondaryLight = Color(0xFF757575);
  static const Color textPrimaryDark = Colors.white;
  static const Color textSecondaryDark = Color(0xFFBDBDBD);

  // Button Colors
  static const Color buttonPrimary = Color(0xFF0D47A1);
  static const Color buttonSecondary = Color(0xFFFFA000);

  // Error and Success Colors
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);

  // Divider and Border Colors
  static const Color divider = Color(0xFFBDBDBD);
  static const Color border = Color(0xFF9E9E9E);

  // Shadow Colors
  static const Color shadow = Colors.black26;
}
